{
  "name": "widget-catalog-registry",
  "version": "1.0.0",
  "private": true,
  "description": "Job agregador del catalogo de widgets: recolecta los widget.meta.json de cada repo, los valida con JSON Schema y produce catalog-index.json.",
  "type": "commonjs",
  "bin": {
    "aggregate-catalog": "./aggregate-catalog.js"
  },
  "scripts": {
    "aggregate": "node aggregate-catalog.js",
    "aggregate:local": "node aggregate-catalog.js --source local",
    "aggregate:remote": "node aggregate-catalog.js --source remote"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
